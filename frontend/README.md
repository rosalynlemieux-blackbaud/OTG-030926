# OTG Frontend Scaffold

This folder contains a SKY UX-oriented Angular scaffold for role-aware navigation and route structure.

## Current status
- Core routing, guards, shell, and placeholder pages are implemented.
- Full Angular CLI workspace files were not generated in this environment because Node/NPM is unavailable.

## Next steps once Node is installed
1. Generate Angular workspace files (or run `ng new` into this folder).
2. Install SKY UX packages (`@blackbaud/skyux` and required SKY modules).
3. Wire API clients to backend endpoints.
4. Replace placeholder pages with SKY UX components and workflows from `plan.md`.
